# Core (mechanistic analysis + training)
# Use torch 2.1.8 wheels on GH200 / aarch64.
torch>=2.0.0
# 4.51+ for Gemma-3 multimodal wrapper (Gemma3ForConditionalGeneration); <5.0 avoids
# lazy-loading that breaks state-dict access used by the mechanistic hooks.
transformers>=4.51.0,<5.0
huggingface-hub>=0.23.2,<1.0
accelerate>=0.30.0
safetensors>=0.4.3
datasets>=2.19.0

# Training helpers
bitsandbytes>=0.43.0          # optional: 8/4-bit teacher inference
peft>=0.11.0                  # optional only; full-parameter FT is preferred

# Rhyme / phonology
pronouncing>=0.2.0
nltk>=3.9.2
wordfreq>=3.0.0               # frequency gate for clean, common rhyme words

# Eval / analysis
scikit-learn>=1.3.0           # CKA helpers, PCA for probe-subspace alignment
scipy>=1.10.0                 # bootstrap CIs, stats
numpy>=1.24.0
matplotlib>=3.8.0
tqdm>=4.66.0

# Misc
argcomplete>=1.9.4
Pillow>=9.1.0                 # transformers Gemma3 image processor import
